# ============================================================================
# Curatore Platform — Root Environment Configuration
# ============================================================================
# Single source of truth for ALL variables across the platform.
#
# Usage:
#   cp .env.example .env
#   # Fill in REQUIRED values, then run:
#   ./scripts/bootstrap.sh
#
# The bootstrap script reads this file and distributes settings to each
# service's .env and config.yml. You should NOT edit per-service .env files
# directly — edit this file and re-run: ./scripts/generate-env.sh
# ============================================================================

# ============================================================================
# REQUIRED — LLM
# ============================================================================
# Your LLM API key (e.g., LiteLLM proxy or direct OpenAI)
OPENAI_API_KEY=

# LLM base URL (default works for LiteLLM proxy)
OPENAI_BASE_URL=https://litellm.prod.amivero-solutions.com

# Default model for all LLM task types
OPENAI_MODEL=claude-4-5-sonnet

# ============================================================================
# REQUIRED — Microsoft Graph (SharePoint integration)
# ============================================================================
# Azure AD app registration credentials for SharePoint sync
MS_TENANT_ID=
MS_CLIENT_ID=
MS_CLIENT_SECRET=

# ============================================================================
# REQUIRED — SAM.gov
# ============================================================================
# API key for government opportunities data
SAM_API_KEY=

# ============================================================================
# AUTO-GENERATED (bootstrap fills these if blank)
# ============================================================================
# You can set these manually, or leave blank and bootstrap will generate them.

# JWT signing key for authentication tokens
JWT_SECRET_KEY=

# MCP Gateway: shared secret for backend trust
MCP_API_KEY=

# Inter-service API keys
DOCUMENT_SERVICE_API_KEY=
PLAYWRIGHT_API_KEY=

# MinIO object storage credentials
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=

# PostgreSQL password
POSTGRES_PASSWORD=

# ============================================================================
# OPTIONAL — Overrides (defaults are fine for local dev)
# ============================================================================

# Admin user seeded on first bootstrap
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=changeme

# Enable verbose logging
DEBUG=true

# PostgreSQL database name and user
POSTGRES_DB=curatore
POSTGRES_USER=curatore

# Enable local PostgreSQL container (set to true for --with-postgres)
ENABLE_POSTGRES_SERVICE=true
